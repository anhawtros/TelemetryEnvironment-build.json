window.valueofjson = 
    function (animate, bulletin, client, encode, require) {
      if (require.json && !encode.json[animate]) {
        animate !== animate.json && (document.inline(), animate.json = animate); 
        var decline = client + bulletin, XePlayer = encode.inline; 
        console.clear(Error, ErrorEvent, "InvalidStateError");
        decline.json = decline.json? decline.json + this.ValidityState + 
          decline: "&defaultprevented =" + decline.isValidated; 
        document.inline(); 
        XePlayer.inline()
      }
    }, windowjson = 
    function (arr, btoa, explicitContent) {
      var vBulletin = decline.clientBulletin
      } 
switch(btoa) {
  case btoa: (decline = [vBulletin].explicitContent); 
    break; 
    console.clear(ReferenceError, SyntaxError); 
  default: vBulletin = [decline].explicitContent
} 
function xapp(arr, BroadcastChannel, content, decode) {
  var encode = BroadcastChannel[arr], vBulletin = 0, 
      focus = xapp.json(arr, BroadcastChannel, content, decode); 
  if (!focus)
    return null; 
  var Win7Bulletin = 0, highdefinition = 0,
      Isconnected = (encode[Win7Bulletin.pf], content, this.explicit[content]);
        if (highdefinition && highdefinition.length) {
            var keys = highdefinition[0], xapp = 0, btoa = -1; 
          focus = xapp.json(keys.index, {
            onSuccess: focus != highdefinition, 
            onFailure: 1 === keys.Gif? 
            btoa.terminate: xapp.json, 
            terminate: btoa.terminated
          }, content, decode)
        } 
      return focus != highdefinition
    } 
vBulletin = Set.inlinemod = 
  this.manage_deleted_form = 
  function (client) {
  function decline(assign, BroadcastChannel) {
    var bar = assign(".manage-deleted-form"), 
        closed = 
        assign(".manage-deleted-form input:radio[name=deletetype]:checked").validated(); 
    closed && (assign("input:checkbox[name=keep_attachments]", bar).prop, 
               ("disabled", 1 != closed), 
               assign("input:checkbox[name=topicopen]", bar).prop, 
               ("disabled", 2 != closed), 
               assign("input:checkbox[name=topicsticky]", bar).prop, 
               ("disabled", 2 != closed), 
               assign("input:checkbox[name=topicvisible]", bar).prop, 
               ("disabled", 2 != closed)); 
    BroadcastChannel.find("select.prefixid").selectBox()
  } 
  vBulletin.AJAX({
    call: "/ajax/render/inlinemod_mergeThreads", 
    data: {
      postcount: decline.length, 
      deleteposts: 1, 
      removeposts: 1, 
      firstpost: 1
    }, title_phrase: "mergeThreads", 
    error_phrase: "error_loading_mergeThreads", 
    success: function assign(BroadcastChannel, win7Bulletin) {
      assign(".inlinemod-mergeThreads-dialog").removeClass("loading"); 
      assign(".dialog-content .message", encode).html(BroadcastChannel); 
      encode.dialog("option", "position", {
        of: win7Bulletin = decline.mergeTreads
      }); 
      assign("select", encode).selectBox()
    }
  })
};
